# Чанк CH-SRC-001-02

```yaml
id: CH-SRC-001-02
источник: SRC-001
точный_локатор: ds.pdf, Приложение B «Инструкция по нагрузочному тестированию»
тема: Инструкция по нагрузочному тестированию
краткое_содержание: >-
  Сервис проверяется системой нагрузочного тестирования по эталонному датасету. Нужно реализовать
  POST /process по контракту. Проверка парами по payload_id: прямая (маскирование) и обратная
  (демаскирование). Метрика маскирования — нормированное span-based расстояние Левенштейна [0..1].
  Таймаут запроса 10 секунд. Ретраи до 2 (итого до 3 попыток), эндпоинт идемпотентен по payload_id.
  429 допустим под нагрузкой (учитывается Retry-After); 429 не увеличивает и не сбрасывает счётчик
  невалидных запросов; успешный ответ сбрасывает счётчик. Если 429 не проходит на маскировании —
  демаскирование по этому элементу не запрашивается; если на демаскировании — учитывается, проверка
  продолжается. Стоп после 5 невалидных запросов подряд. Прогон ~5 минут, целевой RPS 1000, latency
  не более 1 с (ориентир). Проверяются категории ПД из ТЗ.
статус: обработан
связанные_требования: []
связанные_вопросы: []
```